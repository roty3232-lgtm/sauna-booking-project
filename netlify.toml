# netlify.toml

# 1. Configuration du build Next.js sur Netlify
[build]
  command = "npm run build"
  publish = ".next" 
  
# 2. On désactive les fonctions pour l'instant (elles causent l'erreur)
[functions]
  external_node_modules = ["@prisma/client"]